# ⚙️ Настройка Serverless Container в Yandex Cloud

## 🔧 Режим работы контейнера

### ✅ Правильный выбор: **"Выполнение команд"**

**Почему:**
- Бот использует **polling** (long polling) для получения обновлений от Telegram
- Это **долгоживущий процесс**, который постоянно работает
- Бот делает запросы к Telegram API, а не ждет входящие HTTP-запросы
- Команда запуска: `python main.py` (указана в Dockerfile как CMD)

### ❌ Не использовать: "HTTP-сервер"

**Почему:**
- HTTP-сервер предназначен для веб-приложений, которые слушают входящие HTTP-запросы
- Наш бот не является HTTP-сервером
- Если выбрать HTTP-сервер, контейнер будет ждать HTTP-запросы, которых не будет

## 📋 Настройки контейнера

### 1. Режим работы
- ✅ **Выполнение команд**

### 2. Команда запуска
- Можно оставить пустым (используется CMD из Dockerfile: `python main.py`)
- Или явно указать: `python main.py`

### 3. Переменные окружения

Обязательные переменные:

```
BOT_TOKEN=7770460363:AAEGPCzfjdhvmOomQf1FyBoCV3Xyl2etibU
ADMIN_IDS=312551109
GOOGLE_SHEETS_ID=13zmdoS160B5Hn0Cl-q2hNrEgyZVc6Jh0JaxUnI9jSFg
GOOGLE_SHEETS_CREDENTIALS={"id":"ajedivglnfjb9nqskqgr","service_account_id":"ajepfe0asise6kmhf6om",...}
USE_GOOGLE_SHEETS=true
```

### 4. Ресурсы

Рекомендуемые настройки:
- **Память:** 512 MB (минимум) или 1 GB (рекомендуется)
- **CPU:** 0.5 vCPU (минимум) или 1 vCPU (рекомендуется)
- **Таймаут:** 300 секунд (5 минут) или больше

### 5. Docker-образ

```
cr.yandex/crpvi47750ad2ea2hfdb/office-schedule-bot:latest
```

## 🔄 После настройки

1. **Сохраните настройки**
2. **Запустите контейнер**
3. **Подождите 1-2 минуты**
4. **Проверьте логи** - должны появиться сообщения о запуске

## ⚠️ Важно

- Режим "Выполнение команд" означает, что контейнер будет работать постоянно
- Контейнер будет делать запросы к Telegram API каждые несколько секунд (polling)
- Это нормальное поведение для Telegram бота с polling

## 📝 Проверка

После настройки в логах должны появиться:
```
==================================================
🚀 Запуск бота...
==================================================
INFO - Бот запущен...
INFO - Загрузка данных из Google Sheets при старте...
```

Если этих сообщений нет - проверьте:
1. Режим работы установлен на "Выполнение команд"
2. Все переменные окружения установлены
3. Контейнер запущен

