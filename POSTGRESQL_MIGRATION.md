# 🗄️ Миграция на PostgreSQL

## Проблема с Google Sheets

Ошибка `APIError: [429]: Quota exceeded` означает, что мы превысили лимиты Google Sheets API:
- **Read requests per minute per user**: 60 запросов в минуту
- При активном использовании бота это легко превысить

## Решение: PostgreSQL

PostgreSQL - это полноценная база данных, которая:
- ✅ Не имеет таких строгих лимитов на запросы
- ✅ Быстрее работает с данными
- ✅ Поддерживает транзакции и ACID
- ✅ Масштабируется лучше
- ✅ Имеет лучшую производительность

## Варианты развертывания PostgreSQL

### Вариант 1: PostgreSQL на Railway (рекомендуется)

**Преимущества:**
- ✅ Проще настроить
- ✅ Автоматическое резервное копирование
- ✅ Автоматическое масштабирование
- ✅ Удобный интерфейс
- ✅ Интеграция с Railway проектом

**Цены Railway:**
- **Бесплатный план:** $1 кредита в месяц (для новых пользователей $5 на 30 дней)
- **PostgreSQL:** ~$0.50-1.00 в месяц для небольшой БД (256MB RAM, 1GB storage)
- **Hobby план:** $5/месяц (больше кредитов и ресурсов)
- **Pro план:** $20/месяц (для серьезных проектов)

**Для вашего бота:**
- Небольшая БД (сотрудники, расписания) ≈ $0.50-1.00/месяц
- Вместе с приложением ≈ $1-2/месяц
- Укладывается в бесплатный план ($1 кредит) или Hobby ($5/месяц)

**Настройка:**
1. Railway Dashboard → ваш проект
2. **+ New** → **Database** → **Add PostgreSQL**
3. Railway автоматически создаст базу и добавит `DATABASE_URL`
4. Готово!

### Вариант 2: Managed PostgreSQL в Yandex Cloud

**Преимущества:**
- ✅ Полный контроль
- ✅ Можно выбрать конфигурацию
- ✅ Интеграция с другими сервисами Yandex Cloud

**Настройка:**
1. Yandex Cloud Console → **Managed Service for PostgreSQL**
2. **Создать кластер**
3. Выбрать конфигурацию (минимум для начала)
4. Создать базу данных и пользователя
5. Получить строку подключения

**Недостатки:**
- ⚠️ Сложнее настроить
- ⚠️ Нужно управлять резервными копиями вручную
- ⚠️ Платно (но недорого для небольших проектов)

## План миграции

### Этап 1: Подготовка

1. **Выбрать платформу** (Railway или Yandex Cloud)
2. **Создать базу данных**
3. **Получить строку подключения** (`DATABASE_URL`)

### Этап 2: Создание схемы БД

Нужно создать таблицы:
- `employees` - сотрудники
- `admins` - администраторы
- `pending_employees` - отложенные сотрудники
- `schedules` - расписания на даты
- `default_schedule` - расписание по умолчанию
- `requests` - заявки на недели
- `queue` - очереди на дни
- `logs` - логи команд (опционально)

### Этап 3: Миграция кода

1. **Добавить зависимости:**
   - `psycopg2` или `asyncpg` (для async)
   - `sqlalchemy` (опционально, для ORM)

2. **Создать модуль `database.py`:**
   - Подключение к БД
   - Функции для работы с таблицами
   - Миграции схемы

3. **Обновить менеджеры:**
   - `employee_manager.py` → использовать PostgreSQL
   - `admin_manager.py` → использовать PostgreSQL
   - `schedule_manager.py` → использовать PostgreSQL
   - `logger.py` → использовать PostgreSQL (опционально)

4. **Сохранить совместимость:**
   - Оставить Google Sheets как fallback (опционально)
   - Или полностью перейти на PostgreSQL

### Этап 4: Миграция данных

1. **Экспортировать данные из Google Sheets**
2. **Импортировать в PostgreSQL**
3. **Проверить целостность данных**

### Этап 5: Тестирование

1. Протестировать все команды
2. Проверить работу с данными
3. Убедиться, что нет ошибок

## 💰 Стоимость PostgreSQL на Railway

### Бесплатный план:
- **$1 кредита в месяц** (не накапливается)
- Для новых пользователей: **$5 кредита на 30 дней**
- PostgreSQL: **~$0.50-1.00/месяц** для небольшой БД (256MB RAM, 1GB storage)
- Приложение: **~$0.50-1.00/месяц** (зависит от использования)

### Для вашего бота:
- **Небольшая БД** (сотрудники, расписания) ≈ **$0.50-1.00/месяц**
- **Вместе с приложением** ≈ **$1-2/месяц**
- **Укладывается в бесплатный план** ($1 кредит) или **Hobby** ($5/месяц)

### Платные планы:
- **Hobby:** $5/месяц (включает $5 кредита)
- **Pro:** $20/месяц (для серьезных проектов)

### ⚠️ Важно:
После исчерпания ежемесячного кредита ($1) сервисы будут **приостановлены** до следующего месяца или до обновления плана.

## Рекомендация

**Используйте PostgreSQL на Railway:**
- Проще настроить
- Автоматическое управление
- Хорошая интеграция
- **~$1-2/месяц** для вашего бота (укладывается в бесплатный план или Hobby)

## Следующие шаги

1. Создать PostgreSQL на Railway
2. Получить `DATABASE_URL`
3. Начать миграцию кода

---

**Хотите, чтобы я начал миграцию на PostgreSQL?**

