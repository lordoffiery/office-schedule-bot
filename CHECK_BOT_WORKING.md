# ✅ Проверка работоспособности бота в Serverless Container

## 🔍 Шаг 1: Проверить статус контейнера

1. Откройте [Yandex Cloud Console](https://console.cloud.yandex.ru/)
2. Перейдите в **Serverless Containers**
3. Найдите ваш контейнер `office-schedule-bot`
4. Проверьте статус:
   - ✅ **Активен** / **Running** - контейнер работает
   - ⚠️ **Остановлен** / **Stopped** - нужно запустить
   - ❌ **Ошибка** / **Error** - проверьте логи

## 📋 Шаг 2: Проверить логи контейнера

1. В контейнере перейдите на вкладку **Логи**
2. Проверьте последние записи:
   - ✅ Должны быть сообщения о запуске бота
   - ✅ Должны быть сообщения о подключении к Telegram
   - ✅ Должны быть сообщения о загрузке данных из Google Sheets
   - ❌ Если есть ошибки - см. раздел "Типичные ошибки"

### Что искать в логах:

**Успешный запуск:**
```
INFO - Бот запущен
INFO - Загрузка данных из Google Sheets...
INFO - Данные успешно загружены
INFO - Бот готов к работе
```

**Ошибки:**
```
ERROR - BOT_TOKEN environment variable is required
ERROR - Ошибка подключения к Telegram
ERROR - Ошибка загрузки данных из Google Sheets
```

## 🤖 Шаг 3: Проверить бота в Telegram

1. Откройте Telegram
2. Найдите вашего бота (по токену из `BOT_TOKEN`)
3. Отправьте команду `/start`
4. Проверьте ответ:
   - ✅ Бот должен ответить приветствием
   - ✅ Если вы администратор - должны работать все команды
   - ❌ Если нет ответа - проверьте логи и переменные окружения

### Тестовые команды:

1. `/start` - регистрация/приветствие
2. `/help` - список команд
3. `/my_schedule` - ваше расписание (если вы добавлены как сотрудник)
4. `/full_schedule` - полное расписание (если вы администратор)

## ⚙️ Шаг 4: Проверить переменные окружения

1. В контейнере перейдите на вкладку **Переменные окружения**
2. Убедитесь, что все переменные установлены:

### Обязательные переменные:

- ✅ `BOT_TOKEN` - токен Telegram бота
- ✅ `ADMIN_IDS` - ID администраторов (например, `312551109`)
- ✅ `GOOGLE_SHEETS_ID` - ID Google таблицы
- ✅ `GOOGLE_SHEETS_CREDENTIALS` - JSON ключ Google Service Account (минифицированный)
- ✅ `USE_GOOGLE_SHEETS=true`

### Проверка значений:

- `BOT_TOKEN` должен начинаться с цифр и содержать `:`
- `ADMIN_IDS` должен быть числом или списком чисел через запятую
- `GOOGLE_SHEETS_ID` должен быть длинной строкой
- `GOOGLE_SHEETS_CREDENTIALS` должен быть валидным JSON

## 🔧 Шаг 5: Проверить метрики и ресурсы

1. В контейнере перейдите на вкладку **Мониторинг**
2. Проверьте:
   - **CPU** - не должно быть постоянной 100% загрузки
   - **Память** - не должно быть переполнения
   - **Запросы** - должны быть активные запросы при использовании бота

## ❌ Типичные ошибки и решения

### Ошибка 1: "BOT_TOKEN environment variable is required"

**Причина:** Не установлена переменная `BOT_TOKEN`

**Решение:**
1. Контейнер → **Переменные окружения**
2. Добавьте `BOT_TOKEN` с вашим токеном бота
3. Перезапустите контейнер

### Ошибка 2: "Ошибка подключения к Telegram"

**Причина:** Неверный токен или проблемы с сетью

**Решение:**
1. Проверьте правильность `BOT_TOKEN`
2. Убедитесь, что токен активен в @BotFather
3. Проверьте логи на детали ошибки

### Ошибка 3: "Ошибка загрузки данных из Google Sheets"

**Причина:** Неверные учетные данные Google Sheets

**Решение:**
1. Проверьте `GOOGLE_SHEETS_ID`
2. Проверьте `GOOGLE_SHEETS_CREDENTIALS` (должен быть валидный JSON)
3. Убедитесь, что Service Account имеет доступ к таблице
4. Проверьте логи на детали ошибки

### Ошибка 4: Бот не отвечает на команды

**Причина:** Контейнер остановлен или есть ошибки

**Решение:**
1. Проверьте статус контейнера (должен быть "Активен")
2. Проверьте логи на ошибки
3. Убедитесь, что вы добавлены как администратор (если используете админ-команды)
4. Попробуйте перезапустить контейнер

### Ошибка 5: "Permission denied" при работе с Google Sheets

**Причина:** Service Account не имеет доступа к таблице

**Решение:**
1. Откройте Google Sheets
2. Нажмите **Поделиться**
3. Добавьте email Service Account (из `GOOGLE_SHEETS_CREDENTIALS`)
4. Дайте права **Редактор** или **Читатель**

## 🎯 Быстрая проверка работоспособности

### Минимальный тест:

1. ✅ Контейнер в статусе "Активен"
2. ✅ В логах нет ошибок
3. ✅ Бот отвечает на `/start` в Telegram
4. ✅ Команда `/help` возвращает список команд

### Полный тест:

1. ✅ Все переменные окружения установлены
2. ✅ Логи показывают успешный запуск
3. ✅ Бот отвечает на команды
4. ✅ `/my_schedule` возвращает расписание (если вы сотрудник)
5. ✅ `/full_schedule` возвращает полное расписание (если вы администратор)
6. ✅ Админ-команды работают (если вы администратор)

## 📞 Если что-то не работает

1. **Проверьте логи** - там должна быть информация об ошибке
2. **Проверьте переменные окружения** - все ли установлены правильно
3. **Проверьте статус контейнера** - должен быть активен
4. **Попробуйте перезапустить контейнер** - иногда помогает
5. **Проверьте доступ к Google Sheets** - Service Account должен иметь права

## 🎉 Если все работает

Поздравляю! Бот успешно развернут в Yandex Cloud Serverless Container.

Теперь можно:
- ✅ Использовать бота в Telegram
- ✅ Управлять расписанием сотрудников
- ✅ Получать автоматические уведомления
- ✅ Хранить данные в Google Sheets

---

**Следующие шаги:**
- Настройте автоматическое масштабирование (если нужно)
- Настройте мониторинг и алерты
- Настройте резервное копирование (данные уже в Google Sheets)

