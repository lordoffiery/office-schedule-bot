# 🔧 Устранение проблем: Бот не отвечает в Yandex Cloud

## Проблема: Бот не отвечает, логи пустые, статус "active"

### ⚠️ Возможные причины:

1. **Переменные окружения не установлены**
2. **Ошибка при старте (но логи не показываются)**
3. **Контейнер не запускает main.py**
4. **Проблемы с подключением к Telegram**

## 🔍 Пошаговая диагностика

### Шаг 1: Проверить переменные окружения

1. Yandex Cloud Console → **Serverless Containers** → ваш контейнер
2. Перейдите на вкладку **Переменные окружения**
3. Убедитесь, что установлены:

```
BOT_TOKEN=7770460363:AAEGPCzfjdhvmOomQf1FyBoCV3Xyl2etibU
ADMIN_IDS=312551109
GOOGLE_SHEETS_ID=13zmdoS160B5Hn0Cl-q2hNrEgyZVc6Jh0JaxUnI9jSFg
GOOGLE_SHEETS_CREDENTIALS={"id":"...","service_account_id":"...",...}
USE_GOOGLE_SHEETS=true
```

**Важно:** 
- `BOT_TOKEN` должен быть без пробелов
- `GOOGLE_SHEETS_CREDENTIALS` должен быть валидным JSON (минифицированным, в одну строку)
- Все переменные должны быть установлены

### Шаг 2: Проверить команду запуска контейнера

1. В контейнере перейдите на вкладку **Обзор** или **Настройки**
2. Проверьте поле **Команда запуска** или **CMD**
3. Должно быть: `python main.py` или `python3 main.py`

Если команда не указана, Dockerfile должен содержать `CMD ["python", "main.py"]` (уже есть)

### Шаг 3: Проверить логи (детальная проверка)

1. В контейнере перейдите на вкладку **Логи**
2. **Обновите страницу** (F5) - иногда логи обновляются с задержкой
3. Проверьте фильтры:
   - Убедитесь, что выбран правильный период времени
   - Попробуйте выбрать "Все логи" или "Последний час"
4. Если логов все еще нет:
   - Подождите 1-2 минуты после запуска контейнера
   - Попробуйте перезапустить контейнер

### Шаг 4: Перезапустить контейнер

1. В контейнере нажмите **Остановить** (если запущен)
2. Подождите 10-15 секунд
3. Нажмите **Запустить**
4. Подождите 30-60 секунд
5. Проверьте логи снова

### Шаг 5: Проверить через тестовый запрос

Если контейнер настроен с HTTP-триггером, можно проверить:

1. В контейнере перейдите на вкладку **Триггеры**
2. Если есть HTTP-триггер, скопируйте URL
3. Откройте URL в браузере
4. Должен быть ответ (даже если это ошибка)

**Примечание:** Для Telegram бота HTTP-триггер не обязателен, так как бот использует polling.

## 🛠️ Решения типичных проблем

### Проблема 1: Логи пустые, но контейнер активен

**Решение:**
1. Проверьте, что контейнер действительно запущен (не просто "active", а "running")
2. Подождите 1-2 минуты после запуска
3. Попробуйте перезапустить контейнер
4. Проверьте настройки логирования в Yandex Cloud

### Проблема 2: Переменные окружения не применяются

**Решение:**
1. Убедитесь, что переменные сохранены (нажмите "Сохранить" после изменения)
2. Перезапустите контейнер после изменения переменных
3. Проверьте, что нет опечаток в именах переменных

### Проблема 3: Бот не может подключиться к Telegram

**Решение:**
1. Проверьте правильность `BOT_TOKEN`
2. Убедитесь, что токен активен в @BotFather
3. Проверьте логи на ошибки подключения
4. Убедитесь, что контейнер имеет доступ к интернету

### Проблема 4: Ошибка "BOT_TOKEN environment variable is required"

**Решение:**
1. Проверьте, что `BOT_TOKEN` установлен в переменных окружения
2. Убедитесь, что нет пробелов в начале/конце значения
3. Перезапустите контейнер

### Проблема 5: Ошибка при загрузке данных из Google Sheets

**Решение:**
1. Проверьте `GOOGLE_SHEETS_ID` - должен быть правильным ID таблицы
2. Проверьте `GOOGLE_SHEETS_CREDENTIALS` - должен быть валидным JSON
3. Убедитесь, что Service Account имеет доступ к таблице
4. Проверьте логи на детали ошибки

## 📝 Быстрая проверка конфигурации

### Минимальная конфигурация для работы:

```env
BOT_TOKEN=7770460363:AAEGPCzfjdhvmOomQf1FyBoCV3Xyl2etibU
ADMIN_IDS=312551109
GOOGLE_SHEETS_ID=13zmdoS160B5Hn0Cl-q2hNrEgyZVc6Jh0JaxUnI9jSFg
GOOGLE_SHEETS_CREDENTIALS={"id":"...","service_account_id":"...",...}
USE_GOOGLE_SHEETS=true
```

### Проверка через тестовый запрос:

Если хотите проверить, что контейнер вообще работает, можно добавить простой HTTP-эндпоинт для проверки здоровья (health check).

## 🎯 Что делать прямо сейчас

1. ✅ **Проверьте переменные окружения** - все ли установлены?
2. ✅ **Перезапустите контейнер** - остановите и запустите снова
3. ✅ **Подождите 1-2 минуты** - логи могут появиться с задержкой
4. ✅ **Проверьте логи снова** - обновите страницу
5. ✅ **Попробуйте отправить `/start` боту** - возможно, он уже работает

## 📞 Если ничего не помогает

1. Проверьте, что образ действительно собран и загружен в Container Registry
2. Проверьте, что используется правильный образ: `cr.yandex/crpvi47750ad2ea2hfdb/office-schedule-bot:latest`
3. Попробуйте создать новый контейнер с нуля
4. Проверьте документацию Yandex Cloud по Serverless Containers

## 🔍 Дополнительная диагностика

Если логи все еще пустые, возможно:
- Логирование не настроено в Yandex Cloud
- Контейнер не запускает main.py
- Есть ошибка до запуска логирования

Попробуйте:
1. Добавить простой `print("Container started")` в начало `main.py` для проверки
2. Проверить настройки логирования в Yandex Cloud
3. Связаться с поддержкой Yandex Cloud

