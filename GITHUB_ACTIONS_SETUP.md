# Настройка GitHub Actions для автоматического деплоя

## 📋 Пошаговая инструкция

### Шаг 1: Создать Container Registry в Yandex Cloud

✅ **Уже выполнено!**

**Ваш Registry ID:** `crpvi47750ad2ea2hfdb`

### Шаг 2: Создать Service Account

1. Yandex Cloud Console → **IAM** → **Service Accounts**
2. Нажмите **Создать сервисный аккаунт**
3. Укажите имя (например, `github-actions-bot`)
4. Нажмите **Создать**

### Шаг 3: Назначить роль Service Account

1. Откройте созданный Service Account
2. Перейдите на вкладку **Роли**
3. Нажмите **Назначить роли**
4. Выберите роль: **Container Registry → Images Pusher** (`container-registry.images.pusher`)
5. Нажмите **Сохранить**

### Шаг 4: Создать ключ для Service Account

1. В Service Account перейдите на вкладку **Ключи**
2. Нажмите **Создать ключ** → **JSON**
3. **Скопируйте весь JSON** (он будет в одну строку или многострочный - оба варианта подойдут)
4. **Сохраните этот JSON** - он понадобится для GitHub Secrets

### Шаг 5: Добавить секреты в GitHub

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**

#### Секрет 1: `YC_REGISTRY_ID`
- **Name:** `YC_REGISTRY_ID`
- **Secret:** `crpvi47750ad2ea2hfdb`
- Нажмите **Add secret**

#### Секрет 2: `YC_SA_JSON_CREDENTIALS`
- **Name:** `YC_SA_JSON_CREDENTIALS`
- **Secret:** Вставьте весь JSON ключ из файла `yc_sa_key_minified.json` (или скопируйте из `SERVICE_ACCOUNT_INFO.md`)
  - GitHub Secrets поддерживает многострочный JSON, но можно использовать минифицированную версию
  - Минифицированный JSON сохранен в `yc_sa_key_minified.json` (локально, не в Git)
- Нажмите **Add secret**

### Шаг 6: Запустить workflow

#### Автоматически:
- Сделайте любой commit и push в ветку `main`
- GitHub Actions автоматически запустится

#### Вручную:
1. GitHub → **Actions**
2. Выберите workflow **Build and Push to Yandex Cloud**
3. Нажмите **Run workflow** → **Run workflow**

### Шаг 7: Проверить результат

1. После выполнения workflow проверьте логи
2. Если все успешно, откройте Yandex Cloud → Container Registry → ваш registry → Images
3. Должен появиться образ `office-schedule-bot:latest`

### Шаг 8: Создать Serverless Container

1. Yandex Cloud Console → **Serverless Containers**
2. Нажмите **Создать контейнер**
3. Укажите:
   - **Имя:** `office-schedule-bot`
   - **Docker-образ:** `cr.yandex/<YC_REGISTRY_ID>/office-schedule-bot:latest`
   - **Память:** 1 GB
   - **CPU:** 1 vCPU
   - **Переменные окружения:** (см. ниже)

#### Переменные окружения для Serverless Container:

```
BOT_TOKEN=<ваш токен бота>
ADMIN_IDS=312551109
GOOGLE_SHEETS_ID=13zmdoS160B5Hn0Cl-q2hNrEgyZVc6Jh0JaxUnI9jSFg
GOOGLE_SHEETS_CREDENTIALS=<минифицированный JSON>
USE_GOOGLE_SHEETS=true
```

## ✅ Проверка работы

После создания контейнера:
1. Проверьте логи в Yandex Cloud Console
2. Отправьте боту `/start` в Telegram
3. Проверьте, что бот отвечает

## 🔄 Обновление образа

При каждом push в `main`:
- Образ автоматически пересобирается
- Загружается новый образ в Container Registry
- **Важно:** Serverless Container нужно перезапустить вручную или настроить автоматический перезапуск

## 🐛 Troubleshooting

### Workflow не запускается
- Проверьте, что файл `.github/workflows/build-and-push.yml` существует
- Проверьте, что вы делаете push в ветку `main`

### Ошибка авторизации
- Проверьте, что `YC_SA_JSON_CREDENTIALS` содержит полный JSON ключ
- Убедитесь, что Service Account имеет роль `container-registry.images.pusher`

### Ошибка при сборке
- Проверьте логи в GitHub Actions
- Убедитесь, что `Dockerfile` и `requirements.txt` на месте

### Образ не загружается
- Проверьте, что `YC_REGISTRY_ID` указан правильно
- Убедитесь, что Service Account имеет доступ к registry

