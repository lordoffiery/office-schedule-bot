# Office Schedule Bot

Telegram-бот для управления расписанием сотрудников в офисах.

## Описание

Бот помогает управлять распределением сотрудников в офисах с учетом ограничения в 8 посадочных мест в день. Автоматизирует сбор заявок на неделю, формирование расписания и уведомления о свободных местах. Каждое место имеет уникальный идентификатор (например, `1.1`, `1.2`, `1.3` и т.д.), и сотрудники отображаются с указанием их места.

## Функционал

- 📅 Управление расписанием сотрудников с закрепленными местами
- 🔔 Автоматические напоминания в пятницу о необходимости указать дни
- 📊 Анализ свободных мест и уведомления всем сотрудникам
- 📝 Хранение истории расписаний (ретро-периоды и на 2 месяца вперед)
- 👥 Управление списком сотрудников
- 🤖 Автоматическая рассылка финального расписания в воскресенье вечером
- 📊 Интеграция с Google Sheets для постоянного хранения данных
- 🔄 Автоматическое обновление имен сотрудников в расписании при регистрации

## Установка

### Локальная установка

1. Клонируйте репозиторий или скопируйте файлы проекта
2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
3. Настройте `config.py`:
   - `API_TOKEN` - токен вашего Telegram бота (получите у @BotFather)
   - `ADMIN_IDS` - список ваших Telegram ID (можно узнать у @userinfobot)
   - `USE_GOOGLE_SHEETS` - использовать Google Sheets (по умолчанию `True`)
   - `GOOGLE_SHEETS_ID` - ID вашей Google таблицы
   - `GOOGLE_SHEETS_CREDENTIALS` - JSON credentials (или используйте файл `google_credentials.json`)
4. (Опционально) Настройте Google Sheets:
   - Следуйте инструкциям в `google_sheets_setup.md`
   - Создайте Service Account и получите credentials
   - Поделитесь таблицей с Service Account email
5. Запустите бота:
   ```bash
   python main.py
   ```

### Деплой на Railway

1. Подключите ваш GitHub репозиторий к Railway
2. Настройте переменные окружения в Railway:
   - `BOT_TOKEN` - токен вашего Telegram бота
   - `GOOGLE_SHEETS_ID` - ID вашей Google таблицы
   - `GOOGLE_SHEETS_CREDENTIALS` - JSON credentials в минифицированном формате (одна строка)
3. Деплой произойдет автоматически

**⚠️ Важно: Сохранение данных**

Бот использует Google Sheets как основной источник данных, что обеспечивает постоянное хранение между деплоями. Локальные файлы используются только как fallback, если Google Sheets недоступен.

Подробнее о настройке Google Sheets см. `google_sheets_setup.md`.

## Команды

### Для всех сотрудников:
- `/start` или `/help` - справка по командам
  - При первом `/start` сотрудник регистрируется в системе
  - Если сотрудник был добавлен админом, автоматически обновляется расписание с добавлением username
- `/set_week_days [даты]` - указать дни на следующую неделю
  - Пример: `/set_week_days 2024-12-23 2024-12-24 2024-12-26`
  - Также можно: `/set_week_days пн вт чт`
  - Показывает гарантированные дни (из default_schedule) и дополнительные запрошенные дни
- `/my_schedule` - показать свое расписание на текущую неделю
  - Отображает дни в офисе с указанием места (например, "Пн (место 1.6)")
- `/skip_day [дата]` - пропустить день (можно указать несколько дат)
  - Пример: `/skip_day 2024-12-20`
  - Пример: `/skip_day 2024-12-20 2024-12-21`
- `/add_day [дата]` - запросить дополнительный день (можно указать несколько дат)
  - Пример: `/add_day 2024-12-20`
  - Пример: `/add_day 2024-12-20 2024-12-21`
  - Если все места заняты, вы будете добавлены в очередь

### Для администраторов:
- `/full_schedule [дата]` - показать полное расписание на дату
  - Пример: `/full_schedule 2024-12-20`
  - Отображает всех сотрудников с их местами в формате: `1.1: Имя(@username), 1.2: Имя, ...`
  - Сотрудники отсортированы по возрастанию номера места
- `/admin_add_employee [имя] @username` - добавить нового сотрудника
  - Также можно: `/admin_add_employee [имя] [telegram_id]`
  - Или ответить на сообщение пользователя: `/admin_add_employee [имя]`
  - Если имя уже используется, админ получает уведомление
  - Если сотрудник добавлен по username, создается отложенная запись до первого `/start`
- `/admin_add_admin @username` - добавить администратора
- `/admin_list_admins` - показать список администраторов (с username)
- `/admin_skip_day @username [дата]` - пропустить день для сотрудника
  - Пример: `/admin_skip_day @username 2024-12-20`
  - Можно указать несколько дат
- `/admin_add_day @username [дата]` - добавить день для сотрудника
  - Пример: `/admin_add_day @username 2024-12-20`
  - Можно указать несколько дат
- `/admin_set_default_schedule [день] [список сотрудников]` - установить расписание по умолчанию для дня
  - Пример: `/admin_set_default_schedule Понедельник Вася, Дима Ч, Айлар, Егор, Илья, Даша, Виталий, Тимур`
  - Дни: Понедельник, Вторник, Среда, Четверг, Пятница
- `/admin_test_schedule` - тестовая рассылка расписания на следующую неделю (только для админов)

## Как это работает

1. **Пятница 18:00** - бот автоматически отправляет всем сотрудникам напоминание указать дни на следующую неделю
2. **Суббота-Воскресенье** - сотрудники указывают дни через команду `/set_week_days` (можно указать даты или названия дней)
3. **Воскресенье 20:00** - бот анализирует все заявки, формирует финальное расписание и рассылает его всем сотрудникам
4. В сообщении каждому сотруднику указывается:
   - Дни в офисе с указанием места (например, "Пн (место 1.6)")
   - Дни удаленно
   - Дополнительно запрошенные дни и их статус (место найдено/не найдено)
   - Свободные места в дни, которых нет в его расписании

### Работа с текущей неделей

- Сотрудники могут использовать `/skip_day` и `/add_day` для изменения расписания на текущую неделю
- При пропуске дня (`/skip_day`) освобождается место, и первый из очереди автоматически добавляется в расписание
- При запросе дня (`/add_day`) если все места заняты, сотрудник добавляется в очередь
- Очередь обрабатывается автоматически при освобождении места или при формировании расписания на следующую неделю

### Регистрация сотрудников

- Админ добавляет сотрудника через `/admin_add_employee [имя] @username` или `/admin_add_employee [имя] [telegram_id]`
- Если сотрудник добавлен по username, создается отложенная запись в `pending_employees`
- Когда сотрудник пишет `/start`:
  - Если есть отложенная запись, используется `manual_name` из нее
  - Сотрудник регистрируется в системе
  - Автоматически обновляется `default_schedule` в Google Sheets: имя заменяется на форматированное с username (например, "Рома" → "Рома(@rsidorenkov)")
- Сотрудники, добавленные админом, получают полный доступ к функциям бота
- Сотрудники, зарегистрировавшиеся самостоятельно, получают ограниченный доступ и должны быть добавлены админом

## Структура данных

### Google Sheets (основной источник данных):
Все данные хранятся в листах Google Sheets:
- `employees` - список сотрудников (формат: `manual_name:telegram_name:telegram_id:username`)
- `admins` - список администраторов (по одному ID на строку)
- `default_schedule` - расписание по умолчанию (JSON формат с местами: `{"Понедельник": {"1.1": "Имя", ...}}`)
- `pending_employees` - отложенные записи для сотрудников, добавленных по username
- `schedules` - расписания на конкретные даты
- `requests` - заявки сотрудников на недели
- `queue` - очередь на дни
- `logs` - логи всех команд и ответов (сохраняются постоянно, не в локальные файлы)

### Локальные файлы (fallback, если Google Sheets недоступен):
- `data/schedules/` - файлы расписаний на конкретные даты (формат: `YYYY-MM-DD.txt`)
- `data/requests/` - файлы заявок сотрудников (формат: `YYYY-MM-DD_requests.txt`)
- `data/employees.txt` - список всех сотрудников (формат: `manual_name:telegram_name:telegram_id:username`)
- `data/admins.txt` - список администраторов (по одному ID на строку)
- `data/queue/` - очередь на дни (формат: `YYYY-MM-DD_queue.txt`)
- `data/default_schedule.txt` - расписание по умолчанию (JSON формат)

## Расписание по умолчанию

Расписание по умолчанию хранится в формате JSON, где каждому дню соответствует словарь мест:
- **Понедельник**: `{'1.1': 'Дима Ч', '1.2': 'Тимур', '1.3': 'Вася', ...}`
- **Вторник**: `{'1.1': 'Дима Ч', '1.2': 'Тимур', '1.3': 'Вася', ...}`
- И так далее для всех дней недели

Каждый сотрудник имеет закрепленное место на основе приоритета (количество дней в неделю). При отображении расписания сотрудники сортируются по возрастанию номера места, и имена отображаются в формате `Имя(@username)` если есть username.

## Настройка

Все настройки находятся в файле `config.py`:
- `API_TOKEN` - токен Telegram бота (или используйте переменную окружения `BOT_TOKEN`)
- `ADMIN_IDS` - список Telegram ID администраторов
- `REMINDER_HOUR`, `REMINDER_MINUTE` - время напоминания в пятницу (по умолчанию 18:00)
- `SCHEDULE_SEND_HOUR`, `SCHEDULE_SEND_MINUTE` - время рассылки расписания в воскресенье (по умолчанию 20:00)
- `TIMEZONE` - часовой пояс (по умолчанию 'Europe/Moscow')
- `MAX_OFFICE_SEATS` - максимальное количество мест в офисе (по умолчанию 8)
- `USE_GOOGLE_SHEETS` - использовать Google Sheets (по умолчанию `True`)
- `GOOGLE_SHEETS_ID` - ID Google таблицы
- `GOOGLE_SHEETS_CREDENTIALS` - JSON credentials (или используйте файл `google_credentials.json`)

### Переменные окружения для Railway:
- `BOT_TOKEN` - токен Telegram бота
- `GOOGLE_SHEETS_ID` - ID Google таблицы
- `GOOGLE_SHEETS_CREDENTIALS` - JSON credentials в минифицированном формате (одна строка)

Подробный алгоритм работы описан в файле `ALGORITHM.md`.
