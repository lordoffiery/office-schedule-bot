# 🚀 Автоматическое обновление контейнера после сборки

## ✅ Способ 1: Триггер Container Registry (рекомендуется)

Это самый простой и надежный способ. Yandex Cloud автоматически перезапустит контейнер при обновлении образа.

### Настройка триггера:

1. **Yandex Cloud Console** → **Serverless Containers** → ваш контейнер
2. Перейдите на вкладку **Триггеры**
3. Нажмите **Создать триггер**
4. Выберите тип: **Container Registry**
5. Настройте триггер:
   - **Registry:** выберите ваш registry (`crpvi47750ad2ea2hfdb`)
   - **Image name:** `office-schedule-bot`
   - **Tag:** `latest` (или оставьте пустым для всех тегов)
   - **Event types:** выберите `PUSH` (загрузка нового образа)
6. В разделе **Действие:**
   - **Container:** выберите ваш контейнер `office-schedule-bot`
   - **Revision:** выберите ревизию контейнера (обычно `$latest`)
7. Нажмите **Создать**

### Права доступа:

Триггеру нужен Service Account с правами:
- `serverless.containers.invoker` - для вызова контейнера

Если триггер не работает, проверьте права Service Account.

### Как это работает:

1. GitHub Actions собирает и загружает новый образ в Container Registry
2. Триггер автоматически обнаруживает обновление образа
3. Контейнер автоматически перезапускается с новым образом
4. **Никаких ручных действий не требуется!**

---

## ✅ Способ 2: Обновление через Yandex Cloud CLI в GitHub Actions

Если триггер не подходит, можно добавить шаг в GitHub Actions для автоматического обновления контейнера.

### Требования:

1. Service Account с правами:
   - `serverless.containers.editor` - для обновления контейнера
   - Или `serverless.containers.admin` - полные права

2. Добавить секреты в GitHub:
   - `YC_FOLDER_ID` - ID каталога (folder) в Yandex Cloud
   - `YC_CONTAINER_ID` - ID вашего Serverless Container

### Настройка:

1. **Получить Folder ID:**
   - Yandex Cloud Console → выберите ваш каталог
   - Скопируйте ID из URL или из информации о каталоге

2. **Получить Container ID:**
   - Yandex Cloud Console → **Serverless Containers** → ваш контейнер
   - Скопируйте ID контейнера (обычно начинается с `d4e...`)

3. **Добавить секреты в GitHub:**
   - GitHub → ваш репозиторий → **Settings** → **Secrets and variables** → **Actions**
   - Добавьте:
     - `YC_FOLDER_ID` = ваш Folder ID
     - `YC_CONTAINER_ID` = ваш Container ID

4. **Обновить workflow:**
   - Файл `.github/workflows/build-and-push.yml` уже содержит шаг для обновления контейнера (см. ниже)

---

## 📋 Сравнение способов

| Способ | Преимущества | Недостатки |
|--------|-------------|------------|
| **Триггер Container Registry** | ✅ Проще настроить<br>✅ Надежнее<br>✅ Не требует дополнительных прав<br>✅ Работает автоматически | ⚠️ Нужно настроить один раз |
| **CLI в GitHub Actions** | ✅ Полный контроль<br>✅ Можно добавить дополнительные действия | ⚠️ Требует дополнительные права<br>⚠️ Нужно поддерживать секреты<br>⚠️ Сложнее настроить |

## 🎯 Рекомендация

**Используйте Способ 1 (Триггер Container Registry)** - это самый простой и надежный вариант.

---

## 🔧 Настройка прав для Service Account (если нужно)

Если триггер не работает из-за прав:

1. Yandex Cloud Console → **IAM** → **Service Accounts**
2. Найдите Service Account, который используется триггером
3. Перейдите на вкладку **Роли**
4. Нажмите **Назначить роли**
5. Выберите роль: **Serverless Containers → Invoker** (`serverless.containers.invoker`)
6. Нажмите **Сохранить**

---

## ✅ Проверка работы

После настройки:

1. Сделайте любой коммит и push в `main`
2. GitHub Actions соберет новый образ
3. Контейнер автоматически перезапустится (если настроен триггер)
4. Проверьте логи контейнера - должен быть новый запуск

---

## 📝 Примечания

- Триггер срабатывает только при обновлении образа с тегом `latest` (если так настроено)
- Если вы используете другие теги, настройте фильтр в триггере
- Контейнер перезапускается автоматически, но может быть небольшая задержка (несколько секунд)

